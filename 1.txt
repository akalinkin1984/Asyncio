Здравствуйте! Перед asyncio.create_task() поставил await. Проблему не решило.
Да и в лекции не использовали await перед asyncio.create_task().
Ошибки RuntimeError: Event loop is closed возникают даже когда в функции main ни чего нет кроме функции migrate
(т.е. возникает при инициализации моделей). При выполнении инициализации моделей возникает одна ошибка,
а при выполнении всего кода возникает 6 ошибок, но код выполняется полностью.
В файле models.py вроде бы все верно, как в лекции делали.
И на одном компьютере возникает, а на другом нет (я делаю дома и на работе через один репозиторий).
Не понимаю почему. Так же не пойму почему перед вызовом функций
insert_people и get_people не пишем await, они же асинхронные.